# Mission Control — Environment Configuration
# Copy to ~/.mission-control/.env and fill in required values.

# ─── REQUIRED ───────────────────────────────────────────────
# GitHub Personal Access Token (scopes: repo, copilot)
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here

# GitHub repository for agent work (owner/repo format)
GITHUB_REPO=your-org/your-repo

# ─── DATABASE ───────────────────────────────────────────────
# SQLite (default — zero config):
DATABASE_URL=sqlite+aiosqlite:///~/.mission-control/mission_control.db

# PostgreSQL (optional — uncomment and fill in):
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/mission_control

# ─── TELEGRAM (recommended) ────────────────────────────────
# Without Telegram, agents run with no human notification channel.
# Dashboard at :8000 is your only visibility into agent activity.
# TELEGRAM_BOT_TOKEN=your_bot_token
# TELEGRAM_CHAT_ID=your_chat_id

# ─── OPTIONAL MCP SERVERS ──────────────────────────────────
# DigitalOcean (for Quill infra monitoring agent)
# DO_API_TOKEN=your_do_token

# Tavily (web search for agents)
# TAVILY_API_KEY=your_tavily_key

# Twilio (SMS/WhatsApp notifications)
# TWILIO_ACCOUNT_SID=your_sid
# TWILIO_AUTH_TOKEN=your_auth_token
# TWILIO_PHONE_NUMBER=+1234567890

# ─── ADVANCED ───────────────────────────────────────────────
# API host/port
API_HOST=0.0.0.0
API_PORT=8000

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
